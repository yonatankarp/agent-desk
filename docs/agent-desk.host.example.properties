# Agent Desk local host profile template.
# Copy this outside the repository or to an ignored agent-desk.host*.properties file.
# Replace placeholders only in your local copy.

hostAlias=operator-lab
hostEndpoint=<local-http-or-https-endpoint>

# Optional explicit runtime-id to public-alias mappings.
# hostAliasMappings=runtime-primary=operator-lab,runtime-backup=operator-backup

# Optional read-only observation sync settings for local bridge imports.
# hostAuthState=accepted
# hostPermissionMode=read-only-observation
# hostObservationBridge=<local-sanitized-observation-export-json>
