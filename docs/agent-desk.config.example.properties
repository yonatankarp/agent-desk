# Agent Desk public-safe runtime configuration template.
# Copy outside the repository and replace placeholders with sanitized local values.

mode=sample
source=mock

# Use this only with:
# mode=stored-events
# source=local-event-store
# eventStoreLocation=agent-desk-events.ndjson
