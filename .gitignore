.gradle/
build/
.kotlin/
.idea/
*.iml
local.properties
.env
.env.*
!.env.example
*.log
*.tmp
.DS_Store

# Local event/audit store files are runtime artifacts, never project source.
*.ndjson

# OpenClaw runtime files are private workspace context, not project source.
.openclaw/

# Superpowers working docs (specs/plans) are local artifacts, not project source.
docs/superpowers/
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md

# Claude Code local runtime state; only .claude/skills and .claude/agents are project source.
.claude/*
!.claude/skills/
!.claude/agents/
